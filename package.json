{
  "name": "lingee-prototype",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Lingee 高保真原型",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "node build/check-modules.mjs",
    "preview": "npm run build && wrangler dev",
    "format": "prettier --write \"src/**/*.{js,css,html}\"",
    "standalone": "node scripts/build-standalone.cjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.54.8",
    "prettier": "^3.4.2",
    "vite": "^6.0.7",
    "vite-plugin-singlefile": "^2.1.0",
    "wrangler": "^4.131.1"
  }
}
