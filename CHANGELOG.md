# Changelog

本文件记录 `lingee` 原型工程的版本历史，供维护者查阅，按天汇总（不逐条罗列 commit，过程中的小修小补不单独记录）。

版本号遵循 [SemVer](https://semver.org/)：`MAJOR.MINOR.PATCH`。

| 版本  | 日期       | 类型  | 变更内容 |
| :---: | ---------- | ----- | -------- |
| 0.1.6 | 2026-09-02 | PATCH | 环境类型枚举值改为 `XK`（AI 套件）/ `XH`（AI 星瀚），与实现对齐。 |
| 0.1.5 | 2026-09-02 | PATCH | 原型与实现对齐补齐：列表去掉「AccessToken 未启用」标签（认证状态表单内已有表达），新增行不再注入产品类型标签；新增态不再预选数据中心；校验改为一次性报出全部未通过的必填项（原先用 form.reportValidity()，浏览器每次只弹第一个）；环境地址支持归一化（补协议头、去 query/hash 与尾斜杠）并拦截误粘登录页地址；改地址时清空已选数据中心。 |
| 0.1.4 | 2026-09-02 | PATCH | 与实现对齐：列表去掉 AI 套件 / AI 星瀚彩色标签（产品类型在表单内已有字段）；第三方应用 ID 不再预填 lingee-build；「代理用户控制」提示压缩为一行；启用普通 AccessToken 的二次确认正文精简为一句话；锁定字段改为灰底无边框（含 hover / focus 态），编辑与查看态不再自动聚焦只读的环境名；「设为默认环境」勾选框边框加深；「普通 AccessToken 认证」开关与代理用户整体下移到「连接凭证」之后（与实现的字段顺序一致）。 |
| 0.1.3 | 2026-08-31 | PATCH | 环境配置认证口径改为以「是否普通 AccessToken 认证」表达：开关由原「代理用户控制」翻转语义并改名，状态标为「已启用 / 未启用」，代理用户仅在未启用时展示并必填；启用普通 AccessToken 认证增加不可逆二次确认弹窗，明确需先在 ERP 第三方应用关闭「代理用户控制」、且系统无法读取 ERP 侧状态；列表未迁移标签改为「AccessToken 未启用」；移除环境版本过低校验与列表版本标签（适用版本仅在标题区说明）。 |
| 0.1.2 | 2026-08-28 | PATCH | 页面标题改为「金蝶灵基」；侧边栏新增帮助中心图标，点击跳转产品文档；Design System 入口从底部移至用户头像菜单；通知中心列表项间距减半；通知弹窗 tooltip 统一为深色圆角浮层样式；Design System Tooltip 控件 demo 与规范参数更新；Tooltip 归类从「数据展示」修正至「反馈」。 |
| 0.1.1 | 2026-08-13 | PATCH | 修复关联应用悬浮面板自动展开且无法关闭：CSS 拆分时打乱了同特异性选择器的层叠顺序，导致隐藏规则失效。改为按原始层叠顺序合并样式，避免同类问题。 |
| 0.1.0 | 2026-08-12 | MINOR | 原型工程化首日：引入 Vite 构建，产出可双击打开的单文件 `dist/index.html`；单文件原型拆分为 `src/` 模块化结构，单据产物模板独立成文件；设计令牌收敛为单一来源；目录整理，去除与 iCloud 归档重复的内容；环境配置支持编辑/测试连接/复制地址/设为默认/删除。 |
