# 任务详情 V1（2026-09-26）

这是修改最新版前保存的任务详情源码快照。`render-fragment.js.txt` 保存详情主体与右侧字段的原始模板，`execution-detail.js.txt` 保存执行卡片的原始文案，`layout.css.txt` 和 `shell.html.txt` 保存相关布局与页面外壳。运行时在详情页右键可切换到「已保存版本」，原版渲染路径不应用最新版的布局变更。此目录为归档，不参与构建，后续新版迭代不要同步修改。
